name: Node.js CI

on:
  push:
    branches: [packet-update]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: nishekh/bbbstrapi

    steps:
      - run: bash copy.sh
